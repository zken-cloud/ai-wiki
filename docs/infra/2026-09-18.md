# ⚙️ AI Infrastructure — 2026-09-18

*Serving, training, hardware, and the systems layer underneath AI.*

11 items selected.

---

### [Scaling Multi-GPU Video Captioning with PyNvVideoCodec and vLLM](https://vllm.ai/blog/2026-09-18-pynvvideocodec)

**vLLM Blog** · 2026-09-18

vLLM has integrated NVIDIA's hardware-accelerated PyNvVideoCodec video decoder to eliminate CPU decoding bottlenecks during multi-GPU Vision Language Model inference.

- Integrates NVDEC hardware decoders via PyNvVideoCodec 2.0.4 to offload video frame decoding from CPU cores to NVIDIA GPUs.
- Resolves CPU saturation bottlenecks during short-output (100-200 token) video captioning workloads, enabling scaling up to 8 GPUs.
- Requires running the CUDA MPS Daemon (`nvidia-cuda-mps-control -d`) to manage high-concurrency multi-process inference.
- Configured via `--media-io-kwargs` specifying the `pynvvideocodec` backend and dynamic VRAM allocation through `--mm-ipc-gpu-memory-gb`.

**Why it matters:** This feature allows high-throughput video processing pipelines to fully utilize multi-GPU nodes without hitting CPU performance ceilings during video decoding.

`vllm` `pynvvideocodec` `gpu` `hardware-decoding` `multimodal`

---

### [Selecting a vector store for Amazon Bedrock Knowledge Bases](https://aws.amazon.com/blogs/machine-learning/selecting-a-vector-store-for-amazon-bedrock-knowledge-bases/)

**AWS Machine Learning Blog** · 2026-09-17

AWS details selecting customer-managed vector stores for Amazon Bedrock Knowledge Bases, comparing Amazon OpenSearch Service, Amazon Aurora PostgreSQL with pgvector, and Amazon S3 Vectors.

- Amazon Bedrock Knowledge Bases offers both fully managed and customer-managed vector store architectures.
- Amazon OpenSearch Service supports in-memory vector storage, high-dimensional k-NN search, and hybrid lexical-vector search using managed or serverless clusters.
- Amazon Aurora PostgreSQL uses pgvector to support IVFFlat and HNSW indexing, distance metrics (L2, cosine, inner product), and single-precision vectors up to 2,000 dimensions.
- Amazon S3 Vectors provides cloud object storage with native vector support optimized for cost-effective storage.

**Why it matters:** Choosing the right vector store backend allows developers to balance latency, query capability, and cost when deploying RAG architectures on AWS.

`amazon-bedrock` `rag` `vector-database` `aws` `opensearch`

---

### [A shared agentic platform for Wood Mackenzie, on Amazon Bedrock AgentCore](https://aws.amazon.com/blogs/machine-learning/a-shared-agentic-platform-for-wood-mackenzie-on-amazon-bedrock-agentcore/)

**AWS Machine Learning Blog** · 2026-09-17

Wood Mackenzie built APEX, a shared agentic platform using Amazon Bedrock AgentCore to eliminate duplicated infrastructure and scale AI agents to production.

- Addresses internal data showing 88 percent of AI proofs-of-concept fail to reach deployment due to architecture, evaluation, and governance blockers.
- Consolidates three separate applications—Woody, Lens AI, and the ST Trading App—onto a single platform to share tools, memory, and observability.
- Replaces fragmented self-hosted frameworks like LangChain, CrewAI, and n8n with Amazon Bedrock AgentCore as a unified runtime.

**Why it matters:** A shared agentic platform allows enterprise teams to deploy non-deterministic AI agents faster by providing standardized identity, safety guardrails, and observability out of the box.

`amazon-bedrock` `agentcore` `ai-agents` `aws` `enterprise-architecture`

---

### [Optimizing agent system prompts with Amazon Bedrock AgentCore](https://aws.amazon.com/blogs/machine-learning/optimizing-agent-system-prompts-with-amazon-bedrock-agentcore/)

**AWS Machine Learning Blog** · 2026-09-16

Amazon Bedrock AgentCore introduces an automated system prompt optimizer that uses an agentic reflector to analyze production execution traces and generate refined system prompts with accompanying explanations.

- The optimizer uses a Single Agent Reflector model that inspects evaluated execution traces, compares successful versus failed runs, and generates revised system prompts.
- To avoid context window limits from lengthy traces, the optimizer exposes the full trace corpus through a filesystem where the reflector uses shell tools like grep, cat, and diff to analyze data on demand.
- Proposed configuration changes undergo automated platform-level guardrail screening and offline batch evaluation or online A/B testing before promotion.

**Why it matters:** It replaces manual, time-consuming prompt engineering and trace debugging with an automated, data-driven optimization workflow based on actual agent execution patterns.

`aws` `amazon-bedrock` `agentcore` `prompt-optimization` `llm-agents`

---

### [PyTorch Day Japan 2026 Comes to Tokyo on December 10](https://pytorch.org/blog/pytorch-day-japan-2026-comes-to-tokyo/)

**PyTorch Blog** · 2026-09-18

The PyTorch Foundation, Hugging Face, IBM, and Mitsubishi Electric are hosting PyTorch Day Japan 2026 in Tokyo on December 10, with the call for proposals open through September 27.

- Event focuses on PyTorch 2.x, torch.compile, TorchVision, TorchAudio, TorchRL, PyTorch Distributed, and hosted projects including vLLM, DeepSpeed, Ray, Helion, and Safetensors.
- Session tracks cover sovereign local AI, physical/edge AI, vision-language-action models, and real-time on-device inference optimization.
- Call for proposals for presentations and lightning talks closes September 27 at 11:59 PM JST, with schedule announcements on October 14.
- Early registration is available for ¥5,000 until November 11 at 11:59 PM JST, alongside academic pricing.

**Why it matters:** The conference provides a platform for machine learning engineers to share implementations of open-source models, edge AI robotics, and PyTorch 2.x ecosystem tooling.

`pytorch` `machine-learning` `open-source` `edge-ai` `events`

---

### [Implementing defense-in-depth authorization for MCP tools on Amazon Quick](https://aws.amazon.com/blogs/machine-learning/implementing-defense-in-depth-authorization-for-mcp-tools-on-amazon-quick/)

**AWS Machine Learning Blog** · 2026-09-17

AWS details a defense-in-depth authorization pattern using an AWS Lambda interceptor and Amazon Bedrock AgentCore Gateway to enforce tool- and parameter-level OIDC JWT controls for Amazon Quick MCP tools.

- Evaluates OIDC JWT claims from Microsoft Entra ID through four sequential gates: MFA verification, geographic restriction, group-to-role mapping, and tool-level permission checks.
- Uses Amazon Bedrock AgentCore Gateway to provide the HTTP endpoint and JWT validation layer between clients and MCP tools.
- Enforces role-based read/write boundaries (e.g., query-only access for readers vs. admin overrides) and produces immutable audit records in Amazon DynamoDB for mutations.
- Complements standard OAuth 2.0 and SSO identity verification by validating actions and attributes at the specific tool invocation and parameter level.

**Why it matters:** It prevents unauthorized tool execution and data access in Amazon Quick environments by ensuring authenticated SSO tokens are strictly constrained by fine-grained, auditable compliance rules.

`aws` `mcp` `amazon-quick` `authorization` `security`

---

### [Enhancing industrial safety AI with synthetic data on Amazon SageMaker AI](https://aws.amazon.com/blogs/machine-learning/enhancing-industrial-safety-ai-with-synthetic-data-on-amazon-sagemaker-ai/)

**AWS Machine Learning Blog** · 2026-09-17

AWS demonstrated a two-stage synthetic data augmentation pipeline using Amazon SageMaker AI and Amazon Rekognition to generate labeled training images for industrial safety AI without manual annotation.

- Uses Qwen-Image-Edit-2509 on SageMaker AI to insert synthetic people into real scene background images, preserving scale and lighting while avoiding domain gap.
- Employs Amazon Rekognition DetectLabels API for automated bounding-box annotations, eliminating manual tagging costs estimated at $3–$5 per image.
- Achieved up to a 160% improvement in person detection mAP50 for safety-critical edge cases near heavy machinery.

**Why it matters:** It enables safe, cost-effective training of lightweight edge-deployed computer vision models on high-risk, rare safety scenarios without dangerous real-world photography or manual data labeling.

`amazon-sagemaker` `computer-vision` `synthetic-data` `object-detection` `industrial-safety`

---

### [University of Manchester Uses NVIDIA Earth-2 to Forecast Air Pollution Across the UK](https://blogs.nvidia.com/blog/uk-air-pollution-research-earth-2/)

**NVIDIA Blog** · 2026-09-16

Researchers at the University of Manchester adapted NVIDIA Earth-2 generative AI models to perform fast U.K.-wide air pollution forecasting and downscaling.

- Trained the Earth-2 CorrDiff generative downscaling model on Isambard-AI using a single eight-GPU node featuring NVIDIA GH200 Grace Hopper Superchips in two days.
- Generated a high-resolution (2-3 square kilometer) U.K.-wide pollution model trained on one year of hourly chemistry-climate simulation data.
- Integrated Earth-2 StormCast to enable time-dependent forecasts using direct air quality observations.
- Demonstrated inference and test-training workflows running locally on an NVIDIA DGX Spark personal AI supercomputer.

**Why it matters:** By replacing computationally expensive chemistry-based weather models with generative AI downscaling, researchers can rapidly simulate policy changes and run low-latency local forecasts to protect public health.

`nvidia` `earth-2` `air-pollution` `generative-ai` `supercomputing`

---

### [Improving HCLS AI reasoning with open-source agent skills](https://aws.amazon.com/blogs/machine-learning/improving-hcls-ai-reasoning-with-open-source-agent-skills/)

**AWS Machine Learning Blog** · 2026-09-16

AWS released an open-source collection of 38 HCLS agent skills under the MIT-0 license across 11 domains to improve AI reasoning and technical execution through structured SKILL.md documents.

- Skills use the Agent Skills open standard in markdown format (SKILL.md) with YAML frontmatter containing triggers, dependencies, and metadata.
- The taxonomy separates reasoning skills (e.g., ACMG/AMP variant interpretation frameworks) from pipeline skills (e.g., GATK4 HaplotypeCaller command templates).
- Equipped agents achieved a 70-86% win rate in head-to-head comparisons against unequipped agents, with critical thinking win rates reaching 78-85% (d = 0.65-1.03).
- Skills function across over 20 services, including Amazon Bedrock AgentCore, AWS Strands Agents SDK, Claude Code, and OpenAI Codex, without platform-specific customization.

**Why it matters:** Encoding explicit domain decision procedures in human-readable prompts prevents silent reasoning failures in high-stakes healthcare workflows without requiring model retraining or RAG indexing.

`ai-agents` `healthcare` `open-source` `prompt-engineering` `aws`

---

### [Manage end-user OAuth consent for AI agents with Amazon Bedrock AgentCore](https://aws.amazon.com/blogs/machine-learning/manage-end-user-oauth-consent-for-ai-agents-with-amazon-bedrock-agentcore/)

**AWS Machine Learning Blog** · 2026-09-14

Amazon Bedrock AgentCore Identity has introduced Consent portal, a managed web experience that handles OAuth session binding and token storage for AI agents.

- Replaces custom session binding infrastructure with a managed endpoint for AgentCore Gateway, reducing user auth overhead.
- Authenticates users via a corporate IdP using JWT inbound authorization and stores authorized tokens in the AgentCore Identity token vault.
- Enables users to grant per-provider access (such as GitHub or Slack) independently prior to invoking tools.
- Designed to streamline OAuth authorization for IDE and Model Context Protocol (MCP) clients like Cursor, Claude Code, Kiro, and VS Code.

**Why it matters:** Developers no longer need to build and host custom public HTTPS callbacks and session management infrastructure to handle OAuth consent for Bedrock AI agents.

`amazon-bedrock` `oauth` `agentcore` `aws` `ai-agents`

---

### [Monitoring production agent lifecycle with AWS DevOps Agent and AgentCore Evaluations](https://aws.amazon.com/blogs/machine-learning/monitoring-production-agent-lifecycle-with-aws-devops-agent-and-agentcore-evaluations/)

**AWS Machine Learning Blog** · 2026-09-11

AWS demonstrated combining Amazon Bedrock AgentCore Evaluations for live agent quality scoring with AWS DevOps Agent for autonomous infrastructure root-cause analysis in a multi-agent airline reservation system.

- AgentCore Evaluations uses LLM-as-a-Judge methodology to continuously score live interactions on goal completion, correctness, and helpfulness that traditional metrics miss.
- AWS DevOps Agent autonomously traces failures across service boundaries, correlating IAM policies, invocation logs, and orchestration traces to recommend remediations.
- The multi-agent reservation system uses the Strands Agents SDK, AgentCore runtime with built-in OpenTelemetry instrumentation, and Amazon Bedrock foundation models.
- Infrastructure metrics like CloudWatch can stay green during functional failures, such as misscoped supervisor prompts misrouting 20 percent of requests or IAM permission drops causing silent chain failures.

**Why it matters:** It establishes a dual-layer monitoring approach that bridges the gap between infrastructure health and actual generative AI task performance in non-deterministic multi-agent systems.

`aws` `amazon-bedrock` `ai-agents` `opentelemetry` `llm-evaluation`

---
