# ⚙️ AI Infrastructure — 2026-09-16

*Serving, training, hardware, and the systems layer underneath AI.*

14 items selected.

---

### [Dense vs. MoE Models: Active Parameters, Throughput, and When to Choose Each](https://developer.nvidia.com/blog/dense-vs-moe-models-active-parameters-throughput-and-when-to-choose-each/)

**NVIDIA Developer Blog** · 2026-09-15

NVIDIA illustrates how Mixture-of-Experts architectures like Nemotron 3.5 Lightning activate a subset of parameters per token to utilize total model capacity efficiently compared to dense models.

- Mixture-of-Experts (MoE) models route each token to a subset of parameters, such as activating 3B parameters per token in a 30B-parameter model.
- Nemotron 3.5 Lightning serves as an example of an MoE architecture that balances active parameters and overall model capacity.
- Parameter organization in neural networks directly dictates throughput and active parameter count during inference.

**Why it matters:** Understanding MoE architectural tradeoffs allows developers to select models that optimize inference throughput without sacrificing total parameter capacity.

`mixture-of-experts` `dense-models` `nemotron` `llm-architecture` `nvidia`

---

### [How NVIDIA Groq 3 LPX Deterministic Execution Drives Power-Efficient High-Interactivity Inference on NVIDIA Vera Rubin](https://developer.nvidia.com/blog/how-nvidia-groq-3-lpx-deterministic-execution-drives-power-efficient-high-interactivity-inference-on-nvidia-vera-rubin/)

**NVIDIA Developer Blog** · 2026-09-15

The NVIDIA Vera Rubin platform utilizes NVIDIA Groq 3 LPX deterministic execution to maximize performance per watt under strict AI factory power constraints.

- AI factories require full compute platforms to maximize output within strict, limited power budgets.
- Performance per watt serves as the primary metric of value for AI platform performance over raw throughput.
- NVIDIA Vera Rubin integrates Groq 3 LPX deterministic execution to target power-efficient, high-interactivity inference.

**Why it matters:** Optimizing inference for performance per watt allows AI factories to scale compute density within fixed power budgets.

`nvidia` `hardware` `inference` `vera-rubin` `groq`

---

### [How NVIDIA NVLink 6 Delivers Multi-Layer Resiliency for AI Factories](https://developer.nvidia.com/blog/how-nvidia-nvlink-6-delivers-multi-layer-resiliency-for-ai-factories/)

**NVIDIA Developer Blog** · 2026-09-15

NVIDIA NVLink 6 introduces multi-layer resiliency mechanisms to minimize unplanned downtime during large-scale AI training and inference operations.

- Large-scale AI training requires every cluster GPU to synchronize gradients across thousands of collective operations per second.
- Unplanned downtime directly reduces inference request volume, restricting revenue generation.

**Why it matters:** Maintaining continuous cluster uptime is critical to preventing revenue loss and ensuring consistent productivity during high-frequency AI workloads.

`nvlink` `hardware` `ai-infrastructure` `resiliency` `gpus`

---

### [Announcing instance preference lists for Amazon SageMaker AI training jobs](https://aws.amazon.com/blogs/machine-learning/announcing-instance-preference-lists-for-amazon-sagemaker-ai-training-jobs/)

**AWS Machine Learning Blog** · 2026-09-15

Amazon SageMaker AI now supports instance preference lists, allowing users to specify up to five ordered instance types for training and processing jobs to automatically launch on the first available capacity.

- Users can define an ordered priority list of up to five acceptable instance types in a single API call.
- SageMaker AI automatically evaluates the preference list sequentially and launches the job on the first instance type with available capacity.
- Eliminates manual retry loops, status polling, and brittle custom scripts previously needed to handle InsufficientCapacityError failures.
- Supports fallback workflows, such as checking Flexible Training Plans reservations first before falling back to alternative instance types on on-demand capacity.

**Why it matters:** It reduces job start delays and operational overhead by automatically securing alternative GPU compute capacity during peak demand periods without manual intervention.

`aws` `sagemaker` `gpus` `machine-learning`

---

### [How we trained the fastest DSpark for Kimi-K3 using GB300 NVL72](https://vllm.ai/blog/2026-09-15-kimi-k3-dspark)

**vLLM Blog** · 2026-09-15

vLLM's Speculators library trained a 5-layer, 5B-parameter DSpark draft model for the 2.8T Kimi K3, achieving up to ~435 tok/s/user on math reasoning workloads using GB300 NVL72 hardware.

- DSpark combines DFlash's non-causal parallel backbone draft step with lightweight sequential correction components to mitigate suffix decay and intertoken mismatches.
- The Kimi K3 DSpark speculator proposes 8 tokens per step and achieves a macro-average acceptance length of 4.11 tokens across nine evaluation domains.
- Domain-specific acceptance lengths reach 6.42 tokens for mathematical reasoning, 4.96 for HumanEval, and 4.65 for translation.
- Single-stream interactivity increased from ~110 to ~435 tok/s/user on math reasoning, with up to ~3.5× higher output throughput under concurrent load compared to target model execution.

**Why it matters:** DSpark speculative decoding allows massive frontier models like Kimi K3 to achieve significant inference speedups and higher concurrent throughput using accessible, open-source training and deployment pipelines in vLLM.

`speculative-decoding` `vllm` `kimi-k3` `dspark` `inference-performance`

---

### [vLLM x Novita AI: Chord, Faster INT4 MoE for Kimi K2.x. Up to 1.3x on H200, 2.15x on Untuned B300](https://vllm.ai/blog/2026-09-15-novita-chord-w4a16-moe)

**vLLM Blog** · 2026-09-15

Novita AI has open-sourced Chord, a high-performance W4A16 MoE CUDA operator delivering up to 1.3x speedups on NVIDIA H200 and 2.15x on untuned B300 GPUs for Kimi K2.x serving shapes.

- Supports BF16 activations, INT4 weights, group-32 scales, and Kimi K2.x pack-quantized INT4 compressed-tensors checkpoint formats.
- Provides two kernel families: an indexed path compatible with vLLM's Humming backend via `--quantization humming` and a WIP grouped SM90 path.
- Selects optimized execution schedules dynamically based on actual routed tokens per expert given during prefill and decode phases.
- Fixes weight layouts and kernel profiles at load time while tuning schedules dynamically based on per-expert token counts.

**Why it matters:** Chord significantly lowers layer-level latency for INT4 Quantized MoE models like Kimi K2.x on modern Hopper and Blackwell GPUs, boosting inference throughput in vLLM.

`vllm` `cuda` `moe` `quantization` `performance`

---

### [Kimi K3 Performance Optimizations in vLLM: The Road to 2.8× Throughput](https://vllm.ai/blog/2026-09-13-kimi-k3-performance-optimization)

**vLLM Blog** · 2026-09-13

vLLM optimized Kimi K3 serving across scheduling, prefix caching, state recovery, and tensor formats, delivering up to 2.8× end-to-end throughput improvements.

- Achieved up to 180.6% higher throughput (725.0 tok/s at concurrency 16) and reduced average TTFT by up to 85.3% on an 8K/1K workload on a B300 node (TP8, 8-token DSpark speculation).
- Implemented adaptive scheduled-token budgets (PR #51725) and increased default batched token limits from 8,192 to 16,384 for high-memory GPUs (PR #51726), improving TTFT by 55%–65%.
- Optimized model execution using internal KDA prefix checkpoints, zero-copy mixed KDA batches, deferred MXFP4 finalization, ReplaySSM state reconstruction, and decode context parallelism.

**Why it matters:** These stack-wide architectural optimizations significantly lower latency and increase serving capacity for complex Kimi K3 workloads on modern GPU clusters.

`vllm` `kimi-k3` `llm-inference` `performance` `speculative-decoding`

---

### [Scaling Federated Learning Across Docker, Kubernetes, and Slurm with NVIDIA FLARE](https://developer.nvidia.com/blog/scaling-federated-learning-across-docker-kubernetes-and-slurm-with-nvidia-flare/)

**NVIDIA Developer Blog** · 2026-09-15

NVIDIA FLARE scales federated learning projects across heterogeneous infrastructure including Docker, Kubernetes, and Slurm environments.

- Addresses infrastructure operation challenges when expanding federated learning beyond initial single-server, multi-client setups.
- Enables dynamic GPU allocation for active federated learning jobs.
- Provides multi-study isolation to keep research projects separated.
- Preserves local data control and infrastructure governance for participating organizations.

**Why it matters:** Organizations can scale collaborative machine learning models across diverse enterprise compute clusters without compromising site autonomy or resource management.

`federated-learning` `nvidia-flare` `kubernetes` `slurm` `infrastructure`

---

### [Optimizing cost and latency with Amazon Bedrock prompt caching](https://aws.amazon.com/blogs/machine-learning/optimizing-cost-and-latency-with-amazon-bedrock-prompt-caching/)

**AWS Machine Learning Blog** · 2026-09-15

Amazon Bedrock has introduced prompt caching via the Converse API, reducing input token costs by up to 90% and improving time-to-first-token latency for repeated prompt contexts.

- Uses a cachePoint marker in the Converse API to snapshot input content including system prompts, documents, and tool definitions.
- Imposes minimum token thresholds per cache checkpoint, requiring at least 1,024 tokens for Anthropic Claude Sonnet 4.5/4.6 and 4,096 tokens for Opus models.
- Introduces new pricing tiers: cacheWriteInputTokens cost 25% to 100% more than standard input tokens depending on TTL, while cacheReadInputTokens cost 90% less.
- Cache scope is isolated per AWS account and AWS Region, defaulting to a 5-minute TTL with support for up to 1 hour on select models like Anthropic Claude and Amazon Nova.

**Why it matters:** Developers can significantly lower inference costs and latency for multi-turn conversations, RAG workflows, and agentic tasks without reducing context window size or model quality.

`amazon-bedrock` `prompt-caching` `converse-api` `llm-optimization` `aws`

---

### [Agent Substrate brings high-density, scalable, trusted infrastructure to GKE](https://cloud.google.com/blog/products/containers-kubernetes/agent-substrate-available-on-gke/)

**Google Cloud Blog** · 2026-09-15

Google Cloud has announced Agent Substrate on Google Kubernetes Engine (GKE), providing high-density, scalable, and trusted infrastructure for AI agent workloads.

- Integrates specialized infrastructure components into GKE to support high-density execution of AI agents.
- Provides multi-tenant security and isolation features optimized for autonomous agent workloads on Kubernetes.
- Designed to scale GKE cluster environments efficiently to handle dynamic agent lifecycle demands.

**Why it matters:** It enables enterprise platform teams to securely deploy and scale large numbers of AI agents on existing GKE infrastructure without compromising density or security.

`gke` `kubernetes` `ai-agents` `google-cloud` `infrastructure`

---

### [From Megawatts to Tokens: How NVIDIA Maximizes AI Factory Production](https://blogs.nvidia.com/blog/from-megawatts-to-tokens-how-nvidia-maximizes-ai-factory-production/)

**NVIDIA Blog** · 2026-09-15

NVIDIA announced its DSX platform suite, featuring software like DSX MaxLPS and DSX Flex to dynamically manage GPU power budgets and respond to real-time power grid demand signals.

- NVIDIA DSX MaxLPS monitors real-time GPU and rack consumption to recover stranded power capacity, yielding 24% more token throughput within a fixed power budget according to Lambda validation tests.
- NVIDIA DSX Flex processes grid demand-response signals to automatically adjust workload priorities, demonstrated by Emerald AI lowering an active facility's load from 4 MW to 3 MW without dropping critical tasks.
- The NVIDIA DSX suite includes DSX OS for open-source AI factory lifecycle management, DSX Sim for operator simulation before deployment, and validated DSX Reference Designs across compute, storage, and networking.

**Why it matters:** Data center operators can increase AI compute density and token output within existing electrical power limits without waiting for grid transmission expansion.

`nvidia` `data-center` `power-management` `ai-hardware` `grid-flexibility`

---

### [AI Infra Summit: NVIDIA Vera Rubin and DSX Platform Advancements Showcase Energy Efficiencies of Optimizing Tokens Per Watt for AI Factories](https://blogs.nvidia.com/blog/ai-infra-summit-vera-rubin-dsx-energy-efficiencies-tokens-per-watt-ai-factories/)

**NVIDIA Blog** · 2026-09-15

At the AI Infra Summit, NVIDIA highlighted full-stack AI factory advancements, including its Vera Rubin systems, NVHBM custom memory, NVLink Fusion integrations, and DSX software for grid-responsive power optimization.

- d-Matrix integrated its Raptor XPUs with NVIDIA Vera CPUs via NVLink Fusion to deliver low-latency inference.
- Amazon's Annapurna Labs is collaborating with NVIDIA on custom NVHBM high-bandwidth memory technology.
- NVIDIA DSX MaxLPS optimized factory-wide power to increase throughput by up to 1.4x tokens per megawatt, helping Lambda boost performance per watt by 23% on Blackwell servers.
- Emerald AI demonstrated a flexible-load program using DSX Flex with Silicon Valley Power, automatically throttling power on low-priority AI jobs during demand signals while preserving critical workloads.

**Why it matters:** Optimizing AI infrastructure for tokens per megawatt through dynamic power management and grid-responsive load balancing enables data centers to scale compute capacity within strict power grid constraints.

`hardware` `ai-infrastructure` `nvidia` `power-efficiency` `data-centers`

---

### [Scaling Telco Autonomy: Leveraging GNNs with Distributed GraphFlow](https://cloud.google.com/blog/products/databases/run-gnns-at-scale-with-ease-introducing-distributed-graphflow/)

**Google Cloud Blog** · 2026-09-15

Google Cloud has announced Distributed GraphFlow, a framework designed to scale Graph Neural Network (GNN) processing for telecommunications and large-scale network autonomy.

- Distributed GraphFlow enables scalable execution and training of Graph Neural Networks across distributed graph datasets.
- Targeted at telecommunications use cases to improve network automation, topology analysis, and operational efficiency.

**Why it matters:** It allows telecommunications providers to apply advanced GNN architectures to massive, real-world network graphs that exceed single-node memory and compute limits.

`google-cloud` `graph-neural-networks` `telecommunications` `distributed-computing`

---

### [The generative AI customization spectrum: From prompt engineering to custom models on AWS](https://aws.amazon.com/blogs/machine-learning/the-generative-ai-customization-spectrum-from-prompt-engineering-to-custom-models-on-aws/)

**AWS Machine Learning Blog** · 2026-09-14

AWS has detailed an eight-step generative AI customization spectrum categorized into Use, Enhance, and Train stages to help teams choose the appropriate adaptation method based on cost, data, and latency requirements.

- The customization spectrum spans eight steps categorized into three phases: Use (Steps 1-2: prompt engineering and optimization), Enhance (Steps 3-5: RAG, prompt caching, model distillation), and Train (Steps 6-8: fine-tuning, continued pre-training, scratch training via Amazon Nova Forge).
- Model weights remain completely frozen during the Enhance stage, which focuses on surrounding foundation models with external documents, prompt caching, and knowledge distillation.
- The framework provides escalation signals based on accuracy, latency, and domain-specific data needs, advising teams to start at Step 1 because most workloads do not need to escalate past Step 3 (RAG).

**Why it matters:** Following this structured escalation framework prevents teams from over-engineering solutions with costly fine-tuning or under-investing in domain-specific training.

`aws` `generative-ai` `amazon-bedrock` `rag` `fine-tuning`

---
